# is_pydantic_dataclass

**Module:** `pydantic.dataclasses`

## Signature

```python
is_pydantic_dataclass(class_: 'type[Any]', /) -> 'TypeGuard[type[PydanticDataclass]]'
```

## Description

Whether a class is a pydantic dataclass.

Args:
    class_: The class.

Returns:
    `True` if the class is a pydantic dataclass, `False` otherwise.
